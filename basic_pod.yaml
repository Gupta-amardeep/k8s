apiVersion: v1
kind: Pod
metadata:
  name: basic-pod
  labels:
    app: webapp
    type: frontend
spec:
  containers:
  - name: nginx-container
    image: nginx
    ports:
    - containerPort: 8080